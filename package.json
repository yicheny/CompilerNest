{
  "name": "@compilernest/monorepo",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "pnpm -w -F @compilernest/calculator-parser build",
    "demo": "pnpm -w -F @compilernest/calculator-parser demo"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "packageManager": "pnpm@10.8.0",
  "engines": {
    "node": ">=18.12.0"
  },
  "devDependencies": {
    "vitest": "^2.0.5"
  }
}
